# Migrated: Training Runtime Validation

This task moved to `../../projects/training/runtime-validation/`.

Use:

```bash
../../scripts/run_training_runtime_moorethreads.sh
```

The old `run_516_suite.sh` command is kept as a compatibility wrapper.
