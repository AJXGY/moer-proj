# Migrated: Memory Operators

This task moved to `../../projects/operators/memory/`.

Use the repository-level entry point:

```bash
../../scripts/run_operator_mem_moorethreads.sh
```

The old `run_526_suite.sh` command is kept as a compatibility wrapper.
