# Migrated: Training Model Structure

This task moved to `../../projects/training/model-structure/`.

Use:

```bash
../../scripts/run_training_model_structure_moorethreads.sh
```

The old `run_5111_suite.sh` command is kept as a compatibility wrapper.
