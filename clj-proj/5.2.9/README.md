# Migrated: Communication Operators

This task moved to `../../projects/operators/communication/`.

Use the repository-level entry point:

```bash
../../scripts/run_operator_comm_moorethreads.sh
```

The old `run_529_suite.sh` command is kept as a compatibility wrapper.
