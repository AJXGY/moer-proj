# Migrated: Training Time Modeling

This task moved to `../../projects/training/time-modeling/`.

Use the repository-level entry point:

```bash
../../scripts/run_training_moorethreads_smoke.sh
```

The old `run_5214_tp_suite.sh` command is kept as a compatibility wrapper.
