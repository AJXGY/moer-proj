# Migrated: Compute Operators

This task moved to `../../projects/operators/compute/`.

Use the repository-level entry point:

```bash
../../scripts/run_operator_compute_moorethreads.sh
```

The old `run_523_suite.sh` command is kept as a compatibility wrapper.
