# Migrated: Inference Runtime Validation

This task moved to `../../projects/inference/runtime-validation/`.

Use:

```bash
../../scripts/run_inference_runtime_moorethreads.sh
```

The old `run_515_suite.sh` command is kept as a compatibility wrapper.
