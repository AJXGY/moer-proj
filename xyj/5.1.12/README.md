# Migrated: Inference Model Structure

This task moved to `../../projects/inference/model-structure/`.

Use:

```bash
../../scripts/run_inference_model_structure_moorethreads.sh
```

The old `run_512_suite.sh` and `run_512_real_suite.sh` commands are kept as compatibility wrappers.
