# Migrated: LoRA Training Runtime Validation

This task moved to `../../projects/training/lora-runtime-validation/`.

Use:

```bash
../../scripts/run_training_lora_moorethreads.sh
```

The old training shell commands are kept as compatibility wrappers.
