# Migrated: Inference Time Modeling

This task moved to `../../projects/inference/time-modeling/`.

Use the repository-level entry point:

```bash
../../scripts/run_inference_moorethreads_smoke.sh
```

The old `run_5215_suite.sh`, `run_5215_tp_suite.sh`, and `run_5215_real_suite.sh` commands are kept as compatibility wrappers.
