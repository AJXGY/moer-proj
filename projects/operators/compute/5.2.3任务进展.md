# 5.2.3任务进展

- 生成时间：2026-05-12T17:28:08.303278+00:00
- 任务标识：MTT-COMPUTE-OP-SPACE-TEST
- 任务名称：摩尔线程架构计算密集型算子空间维度建模测试

## 当前结论

本次已完成计算密集型算子的空间维度建模验证，但校正后仍存在验证点误差超过 10%，需要继续优化。

## A-F 指标完成情况

| 指标 | 状态 | 说明 |
| --- | --- | --- |
| A | 已完成 | 已在摩尔线程 GPU 服务器上配置建模环境并完成联通检查 |
| B | 已完成 | 已选取 MLP Up/Gate/Down GEMM、FlashAttention、Attention Output Proj GEMM 并确定输入规模 |
| C | 已完成 | 已完成单卡与单机双卡五次实测平均时间采样 |
| D | 已完成 | 已使用算子级空间维度模型输出预测时间，且未追加经验校正 |
| E | 已完成 | 已计算并记录各算子在两种并行规模下的误差值 |
| F | 已完成 | 所有测试算子误差均 ≤ 20%，但未稳定到 10% 内 |

## 关键结果

- 设备后端：musa
- 设备数量：2
- 设备名称：MTT S3000, MTT S3000
- 单卡全体平均吞吐：0.60 TFLOPS
- 双卡全体平均吞吐：1.78 TFLOPS
- 预测后处理：未追加经验校正，T_sim 直接取自主分析工具输出
- 判定结果：通过

## 实测与预测结果

| 算子 | 点类型 | 单卡 T_real(ms) | 单卡 T_tool_raw(ms) | 单卡 T_sim(ms) | 单卡误差 | 双卡 T_real(ms) | 双卡 T_tool_raw(ms) | 双卡 T_sim(ms) | 双卡误差 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| MLP Up GEMM | validation | 591.279 | 609.568 | 609.568 | 3.09% | 303.120 | 319.117 | 319.117 | 5.28% |
| MLP Gate GEMM | validation | 599.881 | 606.579 | 606.579 | 1.12% | 303.288 | 319.055 | 319.055 | 5.20% |
| MLP Down GEMM | validation | 612.892 | 602.270 | 602.270 | 1.73% | 330.407 | 310.129 | 310.129 | 6.14% |
| FlashAttention | validation | 48.935 | 46.491 | 46.491 | 4.99% | 11.803 | 13.349 | 13.349 | 13.10% |
| Attention Output Proj GEMM | validation | 176.051 | 171.777 | 171.777 | 2.43% | 92.850 | 89.074 | 89.074 | 4.07% |

## 结果说明

- 本任务的 `T_sim` 已切换为主分析工具 `projects/shared/train-infer-estimation/torch_operator_mvp.py` 输出。
- 本任务按算子族构造 `calibration_override`：GEMM 算子使用同族 leave-one-out 吞吐参考；FlashAttention 使用额外的 `calib_flash_attention_seq1024` 标定点，不使用自身实测值回填。
- 当前 `MTT S3000` 环境执行 FlashAttention 时，torch_musa 提示 fast FlashAttention kernel 需要 `mp >= 2.2`；本次记录为 `scaled_dot_product_attention` 在当前 `mp 2.1` 环境下的兼容执行路径。
- 当前表格同时保留 `T_tool_raw` 和 `T_sim`；本次 `T_sim = T_tool_raw`，通过结论来自主分析工具原始输出。
- 已移除旧版 GEMM 形状经验校正。旧校正会把已接近实测的 `T_tool_raw` 放大或压低，导致 QKV/Gate/Down 误差异常。
- 误差图中的 20% 红线已按真实纵轴比例重绘，不再使用固定 25% 画布比例。

## 关键产物

- 实测数据：[benchmark_results.json](/home/o_mabin/moerxiancheng-clj-xyj-proj/projects/operators/compute/artifacts/20260415T100500Z/benchmark_results.json)
- 模型结果：[space_model_results.json](/home/o_mabin/moerxiancheng-clj-xyj-proj/projects/operators/compute/artifacts/20260415T100500Z/space_model_results.json)
- 图表汇总：[5.2.3图表汇总.md](/home/o_mabin/moerxiancheng-clj-xyj-proj/projects/operators/compute/5.2.3图表汇总.md)
- 误差图：[error_compare.png](/home/o_mabin/moerxiancheng-clj-xyj-proj/projects/operators/compute/charts/error_compare.png)
- 时间图：[runtime_compare.png](/home/o_mabin/moerxiancheng-clj-xyj-proj/projects/operators/compute/charts/runtime_compare.png)

## 如何复线

```bash
cd /home/o_mabin/moerxiancheng-clj-xyj-proj/projects/operators/compute
bash run_523_suite.sh
```
